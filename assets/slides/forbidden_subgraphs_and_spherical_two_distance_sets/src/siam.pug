include ../../layouts/title_slide.pug
section
  section
    h2 Part I
    h3 Eigenvalues
  section
    h4 Adjacency matrix and smallest eigenvalue
    table
      tr
        td $G$
        td $\lambda_1(G)$
      tr
        td $K_n$
        td $-1$
      tr
        td $K_{1,n}$
        td $-\sqrt{n}$
      tr
        td $P_n$
        td $-2\cos\frac{\pi}{n+1}$
    p.fragment $\lim_n \lambda_1(P_n) = -2$
  section
    p
      | What's the limit of the smallest eigenvalue? 
      span.fragment $-\lambda^*$
    img(src='e2n.png' width=600)
    h3.fragment
      span $\lambda^* = \sqrt{\beta}+1/\sqrt{\beta} = $ 
      span(style="font-size:100%") 2.
      span(style="font-size:90%") 0
      span(style="font-size:80%") 1
      span(style="font-size:70%") 9
      span(style="font-size:60%") 8
      span(style="font-size:50%") 0
      span(style="font-size:40%") 0
      span(style="font-size:30%") 8
      span(style="font-size:30%") 8
      span(style="font-size:30%") 7
      span ...
    p.fragment where $\beta$ is real root of $x^3 = x + 1$.
  section
    p Characterization of graphs with bounded eigenvalues
    p.fragment $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p.fragment
      span.blue Cauchy interlacing theorem
    p.fragment
      | $\mathcal{G}(\lambda)$ is closed under taking subgraphs
      br
      small * all subgraphs are induced
section
  section
    h2 Part II
    h3 Forbidden subgraphs    
  section(data-auto-animate)
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p $\mathcal{G}(\lambda)$ is closed under taking subgraphs
    p Planar graphs are closed under taking minors
    p
      span.blue Wagner's theorem: 
      | no $K_5$ or $K_{3,3}$ minors
  section(data-auto-animate)
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p $\mathcal{G}(\lambda)$ is closed under taking subgraphs
    p
      span.red Question: 
      | Define $\mathcal{G}(\lambda)$ by forbidden subgraphs?
  section(data-auto-animate)
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p
      span.red Question: 
      | Define $\mathcal{G}(\lambda)$ by forbidden subgraphs?
    p.green Simple case $\lambda < 1$
    p.fragment $\lambda_1(K_2) = -1$ hence $K_2 \not\in \mathcal{G}(\lambda)$
    p.fragment $\mathcal{G}(\lambda) = \{$ graphs with no edges $\}$
    p.fragment
      span.red Tautology: 
      | Forbid all graphs outside $\mathcal{G}(\lambda)$
  section(data-auto-animate)
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p
      span.red Question [Bussemaker and Neumaier 1992]
      br
      | Can $\mathcal{G}(\lambda)$ be defined by 
      span.red finite 
      | forbidden subgraphs?
    p.fragment $\mathcal{G}(2)$ is complex; it contains all the line graphs
    p.fragment
      span.blue Cameron, Goethals, Seidel, and Shult: 
      | For every connected graph, it is in $\mathcal{G}(2)$ if and only if it is represented by a subset of $D_n$ or $E_8$ (root systems).
    p.fragment
      span.blue Kumar, Rao and Singhi 
      | $\mathcal{G}(2)$ can be defined by forbidden subgraphs with $\le 10$ vertices
  section
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p Can $\mathcal{G}(\lambda)$ be defined by finite forbidden subgraphs?
    .avatars
      img.avatar(src="jiang.png")
      img.avatar(src="polyanskii.png")
    p
      span.blue J. and Polyanskii 2022+
      br
      | Yes if and only if $\lambda < \lambda^* \approx 2.01980$
  section
    h3 Generalization to signed graphs
    p $\mathcal{G}^\pm(\lambda) = \{$ signed graphs with smallest e.v. $\ge -\lambda\}$
    p.fragment Can $\mathcal{G}^\pm(\lambda)$ be defined by finite forbidden subgraphs?
    p.fragment
      span.blue J. and Polyanskii 2022+ 
      | Yes if and only if $\lambda < \lambda^*$
    p.fragment
      span.blue Motivation [Tidor, Yao, Zhang and Zhao 2022] 
      | Application to spherical two distance sets
  section
    h3 Another application
    p.fragment For every $λ < λ^*$, there exists $n$ s.t. for every symmetric integer matrix $A$ whose diagonal entries are all zero, if $A$ has an eigenvalue less than $-λ$, then so does one of its principal submatrix of order at most $n$. The same conclusion does not hold for any $λ ≥ λ^*$
    p.fragment
      span.blue Vijayakumar: 
      | for $λ = 2$ the natural number $n$ can be as small as (but no smaller than) $10$.
section
  section
    h2 Part III
    h3 Ramsey
  section(data-auto-animate)
    p $\mathcal{G}(\lambda) = \{$ graphs with smallest eigenvalue $\ge -\lambda \}$
    p
      | $\mathcal{G}(\lambda)$ can be defined by finite forbidden subgraphs
      br
      | if and only if $\lambda < \lambda^* \approx 2.01980$
    p.red Break into three cases
    p
      span $\lambda < 2$
      br
      span $2 \le \lambda < \lambda^*$
      br
      span $\lambda \ge \lambda^*$
  section
    p
      span.blue Case 1: 
      | For $\lambda < 2$, $\mathcal{G}(\lambda)$ can be defined by finite forbidden subgraphs
    p.red Ramsey-type result
    p For every $F$ and $\ell, m$ there exists $N$ s.t. for every connected graph $G$ with more than $N$ vertices, if no member in $\{K_{1,4}\} \cup \mathcal{X}(F, \ell, m)$ is a subgraph of $G$ then $F$ is not a subgraph of $G$.
    p Here, the extension family $\mathcal{X}(F, \ell, m)$ consists of
    img(src='ext_family.png')
  section
    p
      span.blue Step 1: 
      | Find $\ell, m$ such that both $\mathcal{X}(C, \ell, m)$ and $\mathcal{X}(D, \ell, m)$ are disjoint from $\mathcal{G}(\lambda)$
    img(src='cd.png' width=300 style='margin: 0')
    p Forbid $\{K_{1,4}\} \cup \mathcal{X}(C, \ell, m) \cup \mathcal{X}(D, \ell, m)$
    p
      span.blue Fact (van Rooij and Wilf): 
      | Every graph that contains neither $C$ nor $D$ as a subgraph is a line graph
    p
      span.red What're left to forbid?
      br
      | Graphs with at most $N$ vertices and line graphs
  section
    p
      span.blue Step 2: 
      | Forbid graphs outside $\mathcal{G}(\lambda)$ with at most $N$ vertices, $P_\ell$, some path-clique/clique extensions of cycles, and some clique extensions of cliques
    p
      span.red What're still left to forbid?
      br
      | Line graphs of rooted trees $T$, where $T$ is of depth $\le \ell$, and all vertices but root of $T$ have degree $\le m$
  section(data-auto-animate)
    p
      span.blue Step 3: 
      | Forbid $L(T)$ for $T$ that is minimal in $\mathcal{F}$, where $\mathcal{F} = \{T \colon L(T) \not\in \mathcal{G}(\lambda)$ depth of $T \le \ell$ and degrees of non-roots are $\le m\}$
    p.red Are there only finitely many minimal $T$ in $\mathcal{F}$?
    p Step 3.1: Enumerate bounded trees $T_1, T_2, \dots, T_n$
    p Step 3.2: Encode each $T \in \mathcal{F}$ by $(t_1, \dots, t_n)$ where $t_i$ is the # of occurrences of $T_i$ as a connected component in the graph obtained by removing the root from $T$.
    p.fragment.red Encodings of minimal $T$ form an antichain in $(\mathbb{N}^n, ≤)$
  section(data-auto-animate)
    p
      span.blue Step 3: 
      | Forbid $L(T)$ for $T$ that is minimal in $\mathcal{F}$, where $\mathcal{F} = \{T \colon L(T) \not\in \mathcal{G}(\lambda)$ depth of $T \le \ell$ and degrees of non-roots are $\le m\}$
    p.red Are there only finitely many minimal $T$ in $\mathcal{F}$?
    p Encodings of minimal $T$ form an antichain in $(\mathbb{N}^n, ≤)$
    p.fragment
      span.blue Dickson's lemma 
      br
      | The poset $(\mathbb{N}^n, ≤)$ does not contain infinite antichains
  section
    p
      span.blue Case 2: 
      | For $2 \le \lambda < \lambda^*$, $G(\lambda)$ can be defined by finite forbidden subgraphs
    .container
      .col
        img(src='forb_dn.png' width=400 style='margin: 0')
      .col
        ul
          li Similar Ramsey-type argument
          li.fragment Use graphs on right in place of $C$ and $D$
          li.fragment Computer assisted proof
          li.fragment $G_{23}$ is critical
section
  section
    h3 Open problems
    p
      span.blue Problem A: 
      | Effectivization
    p.fragment
      span.blue Problem B: 
      | Classify all the connected graphs with smallest eigenvalue in $(-λ^*,-2)$. In particular, classify those that have sufficiently many vertices.
    p.fragment
      span.blue Problem C: 
      | Same problem but for signed graphs
    p.fragment
      span.blue Problem D:
      | Forbidden principle submatrices for $\{-1,0,+1\}$-valued symmetric matrices with eigenvalues bounded from below; connection to Lehmer's Mahler measure problem
if date
  include ../../layouts/contact.pug